# Public Assets

Place your static files (images, fonts, etc.) in this directory. 
They will be served from the root path of your application.

Example: `public/logo.png` will be available at `/logo.png`.